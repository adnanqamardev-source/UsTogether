// Minimal placeholder page to satisfy Next.js build when a pages directory is required.
// The actual UI lives in the App Router (app/ folder). This file will never be rendered
// in production because the app router takes precedence, but its presence prevents the
// "Couldn't find a `pages` directory" error.

export default function Placeholder() {
  return null;
}